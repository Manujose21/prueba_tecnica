# Prueba Técnica

## Instalación
composer install && cp .env.example .env && php artisan key:generate && php artisan migrate && php artisan serve

## Endpoints
- POST /api/products
- GET /api/products
- POST /api/orders
- GET /api/orders/{id}

## Vista
GET /products

